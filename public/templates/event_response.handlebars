<div class="pre-response">
{{#if EventName}}
  ⬑ <a class="event-name" title="UniqueID:{{unity_id}} DatabaseID:{{id}}" href="#responds_to_event/{{in_response_to_event_name}}">{{ in_response_to_event_name }}</a>
{{/if}}

{{#if Requirements}}
  <div class="requirements">
  {{#each Requirements}}
    <span class="requirement" data-name="{{Name}}" data-id="{{ id }}">
      {{#if @first}} if {{/if}}
      <a href="#">{{Name}}</a> = {{FactTestValue}}
      {{#unless @last}} and {{/unless}}
    </span>
  {{/each}}
  </div>
{{/if}}
</div>
